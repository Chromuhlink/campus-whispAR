# macOS
.DS_Store

# SSL Certificates (local dev only)
*.pem

# Python
__pycache__/
*.py[cod]

# IDE
.vscode/
.idea/

# Node
node_modules/
package-lock.json

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log

# Note: Firebase client config (API keys in HTML/JS) is SAFE to commit
# Security is enforced by Firestore Security Rules, not by hiding the API key
# See: https://firebase.google.com/docs/projects/api-keys
